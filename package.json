{
  "name": "onlytty-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser viewer + Node interop tests for OnlyTTY. No build step, no runtime deps.",
  "scripts": {
    "test": "node --test dev/test/web/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
