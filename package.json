{
  "name": "onlytty-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser viewer + Node interop tests for OnlyTTY. No build step, no runtime deps.",
  "scripts": {
    "test": "node --test test/web/"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
